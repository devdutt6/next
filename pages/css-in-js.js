// import styled from "styled-components";

// const Title = styled.h1`
// font-size: '50px';
// color: ${({theme}) => theme.colors.primary };
// `

function CSSJS(){
  return (
  //   <Title>Css in Styled</Title>
    <h1 style={{color:"red"}}>Css IN JS</h1>
  )
}

export default CSSJS;